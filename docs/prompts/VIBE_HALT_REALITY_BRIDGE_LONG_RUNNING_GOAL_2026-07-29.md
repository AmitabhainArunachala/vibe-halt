# VIBE-HALT — Reality Bridge and First External Utility Proof

- **Artifact type:** resumable long-running `/goal` controller
- **Authored:** 2026-07-29
- **Repository:** `AmitabhainArunachala/vibe-halt`
- **Last observed merged `main`:**
  `ab259c07da512af60a8c79fe8888d9c903e0bfd9`
- **Target duration:** 16–24 focused implementation hours across two resumable
  local waves, plus 4–8 hours for any separately authorized target run and all
  human merge/authorization pauses
- **Status:** admission proposal; implementation authority begins only after
  the PR containing this controller is human-merged

## Operator use

Give this whole file to one long-running coding agent. It is the current
critical-path controller after the safe C1–C6 truth/reach phase. Treat every
recorded SHA, PR state, line number, tool version, and external issue state as
stale until refreshed.

Human merge of this controller authorizes only the repository-local packages
and draft PRs described below. It grants no merge, self-approval, spending,
credential, paid-provider, sibling-repository, deployment, unsafe-code, or
foreign-target execution authority.

The executor may prepare a target dossier from public metadata. Running,
cloning dependencies for, instrumenting, or writing into a foreign target
requires a separate operator message naming and approving:

- repository and exact revision;
- license and permitted use;
- local-only data boundary;
- disposable execution environment;
- dependency/network setup boundary;
- fixed test command and resource budget.

No live service, user data, provider key, production endpoint, or destructive
fault is permitted. All target work is local test execution against an
operator-approved revision. If authorization is absent, complete every
repository-local package and stop only the target-execution lane with one
decision-ready packet.

---

# `/goal` VIBE-HALT — REALITY BRIDGE

You are the critical-path builder for `AmitabhainArunachala/vibe-halt`. Your
mission is to convert the proven internal rig into one honest external-utility
path without weakening its evidence doctrine.

This is an implementation campaign, not another architecture essay. Continue
through bounded commits, draft PRs, exact-head CI, review repairs, rebases, and
human merge pauses until the completion contract or one irreducible operator
gate is reached.

## 1. Mission and completion contract

Deliver four mandatory outcomes:

1. **Strict local adapter.** Replace the Python client's quarantine with a
   narrow transport to an explicitly configured Rust engine whose exact
   artifact identity is verified. Python may request work and return typed
   results; it may never infer, mint, or upgrade a verdict, evidence grade,
   digest, or source identity.
2. **Reusable cooperative transport.** Extract the existing child-visible
   cassette protocol from its demo fixture into a documented, bounded,
   reusable D2 boundary. Preserve all 29 open capability channels.
3. **Honest evaluation frame.** Add a versioned pre-registration format for a
   real-defect holdout in which misses and nulls remain in the denominator.
   Keep the existing eleven entries as regression/training assets.
4. **External decision point.** Produce exact admission dossiers for the two
   already-reduced forward-confirmation probes, then—only if separately
   authorized—run at least one and publish the predeclared result, including a
   null.

Use three honest checkpoints:

- **Wave A — local trust bridge (8–12 hours):** R0/R1 only. Freeze the Rust
  machine contract and ship the fail-closed Python adapter.
- **Wave B — reusable boundary and evaluation law (8–12 hours):** R2/R3 after
  Wave A's interface is human-merged. Extract the cooperative D2 transport and
  freeze the holdout/dossier contract.
- **Wave C — conditional external proof (4–8 hours per authorized target):**
  R4 only after R1–R3 are merged and exact target authority exists.

Wave A is the next-build terminal milestone. Do not rush R2/R3 into its time
box. Wave A ends cleanly in exactly one of two states:

- `REALITY_BRIDGE_WAVE_A_DRAFT_READY_FOR_HUMAN_REVIEW`: the bounded R0/R1 draft PR is green
  at its exact head, has no unresolved implementation blocker, and carries
  red-test, negative-matrix, rollback, and evidence receipts; or
- `REALITY_BRIDGE_WAVE_A_MERGED_READY_FOR_WAVE_B`: current `origin/main` contains R0/R1 and a
  fresh merged-main gate passes.

After either state, stop and hand the human the exact merge or resume action;
do not begin Wave B on an unmerged interface. The repository-local campaign
reaches its later readiness pause only when current merged `origin/main`
contains R1–R3 and their full gate battery passes. The Reality Bridge is fully
complete only after one bridge-coupled R4 attempt reaches a predeclared result:

- `FORWARD_CONFIRMED`: at least one approved known target reproduces the same
  reduced mechanism and a fixed control does not;
- `FORWARD_NULL`: every approved attempt reaches its budget without the
  predeclared mechanism;
- `LOCAL_READY_TARGET_AUTHORITY_REQUIRED`: R1–R3 are merged and an exact
  dossier is ready, but the operator has not approved execution. This is a
  truthful readiness pause, not external-utility proof or criterion closure.

`FORWARD_CONFIRMED` validates transfer. It does **not** satisfy the
previously-unknown-bug criterion because both proposed defects were already
published. No agent may self-award human confirmation or unknown-bug credit.

## 2. Truth at admission

Refresh, do not assume, this starting picture:

- safe post-audit C1–C6, CD, V1, and K1 packages are merged (recheck with
  `git log --first-parent --oneline origin/main`);
- Tier 1 has a cross-platform 1,000-replay complete-observation workflow
  (`.github/workflows/verify.yml:386-493`; latest cited run at authorship:
  `https://github.com/AmitabhainArunachala/vibe-halt/actions/runs/30365776537`);
- the C5 child consumes an ordered cassette (`scripts/gate.sh:104-145`);
- the C6 D2 reference campaign records 0 divergent pairs in 100
  (`scripts/gate.sh:146-190`);
- all 29 subprocess capability channels remain `Open`, so D1 is unreachable
  (`docs/specs/SANDBOX_CAPABILITY_ENVELOPE_V1.md:16-33`);
- v2 evidence bundles provide content-addressed self-consistency and semantic
  replay, not signed or authenticated provenance
  (`crates/vh-cli/src/receipts_v2.rs:26-31`,
  `scripts/gate.sh:397-448`);
- fault-plan shrink is exact-fingerprint and resource-bounded only for the
  currently capture-enabled demo workloads (`scripts/gate.sh:449-479`,
  `crates/vh-cli/src/main.rs:99-107`);
- the corpus contains eleven regression entries, five of which reduce
  already-published real issues (run
  `find corpus/entries -maxdepth 1 -type f -name 'VB-*' | sort`);
- previously unknown, independently human-confirmed bugs remain 0/3
  (`docs/audits/REACH_STRATEGY_DEBATE_PACKET_2026-07-25.md:64-71`);
- the Python runner, sandbox, and CLI remain quarantined, but the package still
  exports legacy simulator and caller-constructible evidence surfaces
  (`clients/python/vibe_halt/core/runner.py:10-30`,
  `clients/python/vibe_halt/__init__.py:5-9`,
  `clients/python/vibe_halt/core/evidence.py:24-62`);
- C7 supervisor admission remains deferred
  (`docs/audits/C7_ADMISSION_LEDGER_2026-07-25.md:9-14`);
- guided-exploration investment remains stopped
  (`docs/audits/antithesis-dst-2026-07-21/CONVERGENCE_LEDGER_2026-07-22.md:240-256`).

If any statement has changed, record the exact evidence and re-plan before
editing. Never use this controller to overwrite newer merged law.

## 3. Preflight and admission

Read `AGENTS.md` and `CLAUDE.md` in full from the existing checkout, then run
the required local admission command before any other non-trivial read or
edit:

```bash
make onboard
git status --short
git rev-parse HEAD
```

Only then refresh remote state, reconcile or create the authorized worktree
from current `origin/main`, and rerun admission on that re-anchored checkout:

```bash
git fetch origin
git rev-parse origin/main
make onboard
make gate
```

Also record:

```bash
rustc --version
cargo --version
python3 --version
git worktree list --porcelain
```

Resolve all open PRs: base/head SHAs, draft state, mergeability, checks,
reviews, unresolved threads, and changed surfaces. Do not stack on a stale or
conflicting implementation PR. PRs #25 and #29 were stale drafts at
authorship; neither is a dependency, and this controller grants no authority
to merge, close, or repurpose them.

Then read in full:

- `VISION.md` and `README.md`;
- `docs/governance/ACTIVE_TRACK.yaml`;
- `docs/plans/VIBE_HALT_BUILD_PLAN_2026-07-20.md`;
- `docs/specs/DETERMINISM_TIERS.md`;
- `docs/specs/SANDBOX_CAPABILITY_ENVELOPE_V1.md`;
- `docs/audits/REACH_STRATEGY_DEBATE_PACKET_2026-07-25.md`;
- `docs/audits/C7_ADMISSION_LEDGER_2026-07-25.md`;
- `corpus/SCHEMA.md` and `corpus/PLAYBOOK.md`;
- `clients/python/pyproject.toml`, every file under
  `clients/python/vibe_halt/`, and every Python test; inventory the built wheel
  as well as the source tree;
- `crates/vh-cli/src/{main,bundle,receipts_v2,sandbox_demo,sandbox_campaign}.rs`;
- `crates/vh-sandbox/src/{lib,capability,cassette_v2}.rs`;
- `scripts/gate.sh`.

Run `make gate` before the first code edit. If the baseline is red, create no
feature diff until the failure is classified as baseline, environment, or
regression with exact evidence.

## 4. Governance, ownership, and parallelism

Use the three existing active tracks. Do not create a fourth.

| lane | owner | primary surfaces | may run in parallel with |
|---|---|---|---|
| R0/R1 adapter | `vibe-halt-core-2026-07` | `clients/python/**`, `crates/vh-cli/**` | read-only R3 public dossier research only |
| R2 cassette transport | `vibe-halt-core-2026-07` | `crates/vh-sandbox/**`, narrow CLI fixture call sites | R3, but only after R1's interface is human-merged |
| R3/R4 evaluation | `vibe-bug-corpus-2026-07` | `corpus/**`; public metadata read-only until authorized | dossier research during R1; schema/cohort writes only after R1 merge |
| independent falsification | `vibe-halt-verify-2026-07` | verifier-owned tests/workflow only after an exact interface request | any package after interface freeze |

One writer owns each file. Shared `scripts/gate.sh`,
`docs/governance/ACTIVE_TRACK.yaml`, `Cargo.toml`, and `Cargo.lock` are
serialized under one named integration writer. Never solve a collision by
silently broadening ownership.

Human merge of the admission PR containing this controller ratifies exactly
one non-append-only governance rebaseline in that same diff: for tracks
`vibe-halt-core-2026-07` and `vibe-bug-corpus-2026-07`, replace their stale
acceptance/`next`/`non_goals` entries with the Reality Bridge and independent
holdout contract, and add `VISION.md` to the core-owned surfaces. This is the
sole exception to `ACTIVE_TRACK.yaml`'s append-only shared-surface protocol.
It authorizes no other ownership, track, WIP, or governance mutation; any
different diff needs a new enumerated human-merged exception.

Recommended execution topology:

- **Coordinator/integrator:** live-state reconciliation, protocol freeze,
  shared gates, PR boundaries, final evidence capsule.
- **Adapter builder:** R1 implementation and Python/Rust contract tests.
- **Boundary falsifier:** adversarially review R1 during Wave A; begin R2
  extraction and hostile-input/leak tests only after R1's interface is
  human-merged.
- **Evaluation builder:** public, read-only dossier research during Wave A;
  holdout-schema writes only after R1 is human-merged; conditional R4 only
  after every R4 entry gate is true.

Agents exchange exact commit SHAs and interface contracts, not prose claims.
No agent merges or approves another agent's PR.

## 5. Work packages

### R0 — Freeze the bridge contract with red tests

**Goal:** make false success mechanically impossible before implementing a
happy path.

Specify one versioned machine contract owned and verified by Rust. The exact
encoding is an implementation decision, but it must:

- carry request/result schema versions, a versioned operation identifier,
  required feature identifiers, engine/build identity, request identity,
  evidence tier/grade, verdict, exit classification, receipt location, and
  receipt digest;
- have Rust publish the exact supported operation/feature set and reject every
  unknown or unmet required feature before execution;
- distinguish an operator-requested source revision from a mechanically
  observed source revision; the latter remains `UNKNOWN` unless independently
  resolved;
- bind the exact engine artifact by content digest or a trusted build receipt,
  expose the mechanically observed engine digest, and reject a mismatch before
  returning any checked verdict;
- distinguish `CLEAN`, `FINDINGS`, `UNCHECKED`, usage failure, execution
  failure, and evidence failure without collapsing them into booleans;
- be emitted atomically only after Rust has verified the evidence it names;
- reject duplicate, unknown, reordered, missing, malformed, truncated, or
  trailing fields;
- bind every filesystem path to a caller-provided fresh output root;
- carry observed source revision as `UNKNOWN` when it was not independently
  resolved, never by copying the caller's requested revision;
- preserve D0/D1/D2 and Tier-1/Tier-2 labels exactly.

Add failing tests first for at least:

1. missing engine binary;
2. wrong engine version or schema;
3. non-empty output directory;
4. missing result record;
5. exit/result mismatch;
6. malformed, duplicate, unknown, reordered, truncated, or trailing data;
7. tampered receipt or digest;
8. path escape, symlink escape, or receipt outside the output root;
9. timeout or output truncation;
10. `UNCHECKED` incorrectly mapped to clean;
11. finding incorrectly mapped to clean;
12. D2 receipt upgraded to D1;
13. caller-supplied digest or grade trusted without Rust verification;
14. stale result from a previous invocation.

Acceptance: the pre-implementation tests fail for the intended reason and the
failure output is recorded. No production code before this red receipt.

### R1 — Strict Python-to-Rust evidence adapter

**Goal:** replace quarantine with a useful but narrow client.

Implement the smallest API that can:

- make the installed `vibe_halt` package a client only: no Python-side fault
  injector, property oracle, RNG simulator, or caller-authored evidence path;
- locate an explicitly configured or package-approved `vh` executable;
- invoke it without a shell, with an argument vector and bounded timeout;
- allocate or require a fresh output directory;
- submit an immutable versioned request through one stable Rust machine
  command, naming an operation and required features;
- negotiate only against the Rust-published operation/feature set and fail
  closed before execution when a requirement is unsupported;
- obtain the Rust-owned machine result;
- ask Rust to verify any persisted receipt before returning;
- map the verified result to a closed Python outcome type;
- expose evidence paths and digests as read-only data;
- preserve stdout/stderr only as diagnostic attachments, never as truth.

The Python layer must not:

- export or package any legacy second-simulator surface as a supported API;
- parse human-oriented banners or search stdout for `CLEAN`;
- recompute or bless the Rust digest as an authority substitute;
- accept a caller-provided verdict, tier, or capability grade;
- copy or promote caller-provided `requested_source_revision` into
  `observed_source_revision`; the former is untrusted request metadata that
  may be transported, while only independent resolution may populate the
  latter;
- silently retry a finding, divergence, timeout, or malformed receipt into
  success;
- create fake demo receipts;
- import `dharma_swarm` as a production dependency;
- claim the end-to-end adapter criterion before a real integration receipt.

Use Python's standard library only. If an external dependency appears
necessary, stop R1 and request a named operator-ratified exception in a
separate admission PR with its determinism review; absent that exception, the
dependency is forbidden. Remove `pydantic` from `project.dependencies` and
make the runtime dependency list empty; do not add or change a build backend
without that same exception. Preserve the package's advertised Python 3.10+
compatibility, or make any version-floor change an explicit separately
reviewed metadata and fail-fast decision. Add unit and subprocess contract
tests for the full R0 negative matrix plus one positive clean run, one
finding, and one unchecked run generated by the actual Rust engine.

Keep the public surface intentionally small. A suggested shape is an immutable
request, immutable engine-result data, typed verdict enum, and one runner
method; the names are not law. `EvidenceReport` must be de-exported. Treat
every Python object constructed, copied, monkeypatched, or deserialized
outside the runner as untrusted data: no supported API may consume it as
verified. A trusted decision must invoke Rust verification and bind the exact
per-invocation request identity, observed engine digest, receipt digest, and
result bytes. Constructor guards may prevent accidental misuse but are not an
authority seal. Delete the legacy `faults.py`, `properties.py`, and `rng.py`
modules if unused; otherwise move them outside the installed package. Merely
adding deprecation warnings is not sufficient.

Acceptance:

- all old quarantine tests are intentionally replaced, not simply deleted;
- the installed-wheel inventory contains no legacy simulator modules and its
  runtime dependency list is empty;
- `vibe_halt.__all__` exposes only the request/runner/typed engine-result
  surface; hostile construction/import/copy/monkeypatch tests prove no
  supported runner or consumer path returns or accepts `CLEAN`, a grade, or a
  verified identity without a fresh Rust verification bound to the exact
  invocation and bytes;
- Python result objects are documented as data rather than authority, and
  Python returns clean only from the Rust-verified runner path;
- every other outcome remains typed and fail-closed;
- no test uses a hand-written "successful" receipt as its positive oracle;
- direct CLI and Python-client execution point to the same receipt digest and
  verdict for the same request.

### R2 — Reusable child-visible cassette transport

**Goal:** turn the C5 fixture protocol into a reusable cooperative D2 seam
without pretending it is containment.

Extract the ordered request/response framing, identity binding, consumption
accounting, size limits, and taint semantics from the demo-only fixture.
Keep one canonical protocol implementation or generator; do not fork a second
near-equivalent wire format into the Python client.

Required properties:

- R2 registers a versioned cooperative-target operation and feature set behind
  R1's already-public generic request/runner surface; it may make a reviewed
  Rust request-schema version bump, but it must not require a target-specific
  Python API or teach Python to interpret transport truth;
- parent and child agree on cassette schema and digest before work;
- request order and duplicate occurrences remain distinct;
- miss, extra request, unconsumed history, malformed frame, identity mismatch,
  timeout, or truncation taints the run `UNCHECKED`;
- byte/count bounds are checked before allocation;
- every parent-created transport path and descriptor is rooted, validated,
  bounded, and closed by the controller; an attempted or observed child escape
  taints the result `UNCHECKED`;
- diagnostics never contain secrets or uncontrolled environment dumps;
- the capability receipt still lists all 29 channels as open;
- arbitrary child filesystem and descriptor access remains an explicitly open
  D2 capability; this package makes no containment guarantee;
- the reference C6 campaign remains byte/replay compatible unless a deliberate
  schema bump is separately justified.

Add a tiny cooperative target fixture independent of the old demo harness.
Prove exact replay, miss, leftover, malformed input, identity mismatch, and
bounded-resource behavior through public APIs. An integration test must invoke
the new R2 operation through the unchanged public R1 Python runner and prove
that direct CLI and Python paths bind the same Rust-owned receipt digest.

Kill R2 and retain the demo-only protocol if extraction requires unsafe code,
platform-specific containment claims, a new external dependency, frozen
identity drift, or a second truth authority. Publish the exact reason; do not
paper over it.

### R3 — Holdout law and exact forward-confirmation dossiers

**Goal:** make external evaluation measurable before seeing results.

The acceptance population remains real defects whose introducing change has
documented AI/agent authorship in a disclosed PR or commit. Mere later review,
testing, or maintenance by an agent does not qualify an otherwise conventional
defect. Generic conventional-code defects may be useful calibration cases but
cannot satisfy criterion 3.

An independent holdout curator—not the adapter/engine builder—owns candidate
identity, eligibility, and the ground-truth oracle. Before the curator reveals
the cohort, publish the exact engine commit, adapter commit, executable digest,
protocol schemas, evaluation policy, named roles, and a curator-authenticated
opaque commitment to the already-selected cohort bytes in a candidate-secret
freeze artifact that is human-merged to public `main`. The curator then
publishes a separately authenticated reveal artifact whose bytes open that
commitment and bind to the merged freeze commit/digest. Public Git ancestry,
commitment verification, and authentication receipts—not self-declared
timestamps—prove the ordering. After reveal:

- no candidate-specific engine, adapter, fault model, or oracle change is
  permitted for that cohort;
- the evaluator executes and classifies independently from the builder;
- only the first complete execution under the pre-reveal freeze may earn
  criterion-3 acceptance credit;
- any later engine/adapter/schema change may rerun the entire revealed cohort
  only as versioned calibration with `acceptance_credit=false`; the original
  score and state history remain visible;
- a later acceptance attempt requires a newly curated, previously unseen
  holdout and a new public freeze→authenticated-reveal sequence; and
- the two already-known VB-008/VB-010 calibration dossiers below receive no
  holdout credit.

Add a versioned holdout manifest contract separate from admitted regression
entries. Before cohort freeze, a dossier is exactly `DRAFT`,
`NOT_ADMISSIBLE`, or `ADMISSIBLE`, and every `ADMISSIBLE` dossier is assigned
exactly one immutable `cohort_kind`: `HOLDOUT` or `CALIBRATION`. Only
`ADMISSIBLE` dossiers may enter a named immutable cohort. A `HOLDOUT` cohort
contains at least 25 candidates and is the only kind eligible for criterion-3
credit—and only on its first complete execution under its pre-reveal freeze.
Once revealed, every later run of that cohort is calibration with
`acceptance_credit=false`. A `CALIBRATION` cohort has no minimum size and can
exercise the bridge without entering the acceptance denominator. VB-008 and
VB-010 are permanently calibration candidates because they were known before
this contract. Each frozen candidate in either kind then has exactly one
current execution state: `UNRUN`, `AUTHORITY_BLOCKED`, `DETECTED`, `MISS`, or
`INVALID`, with an append-only state history.

The candidate-secret freeze artifact must record:

- independently evidenced curator and evaluator identities/roles plus the
  engine/adapter builder identity; curator and evaluator must be independent
  from the builder;
- exact engine commit, adapter commit, executable digest, protocol-schema
  digests, evaluation-policy digest, fixed budget policy, and the allowed
  oracle contract;
- a closed sampling frame, search start/end cutoff, deterministic selection
  algorithm, curator-secret selection-seed commitment, eligibility/exclusion
  rules, deduplication and root-cause clustering rule, and replacement policy;
- diversity floors of at least five repositories and five root-cause/mechanism
  clusters, with no repository or cluster contributing more than
  `ceil(0.2 * N)` candidates;
- a curator-authenticated `cohort_commitment_digest` over the exact canonical
  reveal bytes selected under that policy;
- its own content digest and eventual human-merged public-main commit; and
- no plaintext candidate IDs, repository names, revisions, or mechanisms.

The later curator-authenticated reveal artifact must record:

- the exact ancestor freeze commit and digest plus the authentication method,
  verification identity/key, and verification receipt;
- the revealed selection seed and the canonical cohort bytes whose digest
  exactly opens the pre-reveal `cohort_commitment_digest`;
- the complete sampling-frame screening ledger, including every included and
  excluded candidate, the deterministic selection result, deduplication/
  cluster assignment, and predeclared exclusion reason;
- stable candidate ID and provenance;
- documentary evidence that the affected code was AI-generated or
  agent-authored in the introducing PR/commit; absent or merely
  post-introduction agent involvement makes the dossier ineligible for the
  acceptance cohort;
- repository, exact pre-fix revision, and fixed-control revision;
- license;
- defect statement and independent ground-truth oracle;
- the specific reduced mechanism the rig claims to transfer;
- allowed local injection seam;
- fixed campaign/test command, universe/attempt budget, timeout, and resources;
- immutable cohort ID, candidate ID, `cohort_kind`, derived
  `acceptance_credit`, and the state vocabulary above;
- receipt/digest fields populated only after execution;
- confirmation source and unknown/known classification.

The reveal must be a signed commit/tag or carry another independently
verifiable curator attestation bound to its bytes. Its exact freeze commit
must already be an ancestor of public `main`; authored timestamps alone prove
nothing. A single commit may not manufacture both stages, and reveal bytes
that do not open the pre-reveal cohort commitment are rejected.

No candidate can leave a frozen cohort. An `INVALID` candidate must match an
exact invalidation condition declared before reveal and remains in the primary
denominator; publish the invalid count separately. No post-freeze exclusion
changes the acceptance metric. Any
post-registration correction creates a new manifest version while preserving
the immutable original cohort and every prior state. For frozen cohort size
`N`, primary recall is `DETECTED / N` and success requires at least
`ceil(0.8 * N)` detections. This is never recall over admitted regression
entries. After the first complete scored run, that cohort's
`acceptance_credit` becomes permanently false for all later implementation
versions.

The state layers and mapping are fixed:

| layer | states | rule |
|---|---|---|
| pre-cohort dossier | `DRAFT`, `NOT_ADMISSIBLE`, `ADMISSIBLE` plus `HOLDOUT` or `CALIBRATION` kind when admissible | only `ADMISSIBLE` can enter a cohort; calibration never earns acceptance credit |
| frozen candidate in either cohort kind | `UNRUN`, `AUTHORITY_BLOCKED`, `DETECTED`, `MISS`, `INVALID` | every row and state transition remains visible; acceptance cannot be claimed while a holdout row is `UNRUN` or `AUTHORITY_BLOCKED` |
| bridge execution | `FORWARD_CONFIRMED`, `FORWARD_NULL`, `FORWARD_INVALID` | maps respectively to `DETECTED`, `MISS`, `INVALID`; invalid never completes external proof |

Prepare two decision-ready dossiers from the existing reductions:

1. **VB-008 / LangGraph issue #6491:** force the reduced write-side validation
   gap and checkpoint-recoverability mechanism, not merely a vaguely similar
   invalid-output symptom.
2. **VB-010 / LangGraph issue #7361:** force resume-at-most-once failure at the
   relevant pre-fix boundary, with a fixed control that distinguishes replay
   from intended resume.

Resolve current public state and exact reproducible pre-fix revisions. If an
issue cannot be tied to an exact permissible revision and independent oracle
before cohort freeze, mark the dossier `NOT_ADMISSIBLE` with the missing fact.
Never guess a SHA from an issue number or choose a revision because it produces
the desired result.

The dossier may include setup commands, but do not execute them without the
separate target authorization defined at the top of this controller.

### R4 — Conditional foreign-target confirmation

**Entry gate:** current merged `origin/main` contains the exact reviewed R1,
R2, and R3 commits; a fresh merged-main `make gate` passes; and the operator
has separately authorized one named R3 dossier under the authority contract at
the top of this controller. Any missing predicate stops R4.

Run only in a fresh disposable worktree or temporary environment. Keep
credentials absent and network disabled during the test after dependencies
are acquired. Use conventional test seams such as a local fixture,
monkeypatch, mock, or process signal at an explicit hook. Do not contact live
providers or services.

For each authorized target:

1. verify checkout SHA and tree before setup;
2. record tool and dependency versions;
3. execute the unmodified treatment through the public R1 adapter and R2
   transport, producing a Rust-owned machine result and verified receipt;
4. apply the predeclared local-only injection and execute it through that same
   bridge within the fixed budget;
5. run the fixed/repaired control revision through the identical R1/R2
   protocol;
6. bind target SHA, engine digest, request identity, oracle, budget, treatment,
   control, and all receipt digests into the result;
7. persist commands, exits, bounded diagnostics, and Rust-verified evidence;
8. map the frozen holdout or calibration candidate state exactly: `DETECTED` to
   `FORWARD_CONFIRMED`, `MISS` to `FORWARD_NULL`, and `INVALID` to a
   non-completing `FORWARD_INVALID` result.

A mere reproduction of the issue's published symptom does not count if the
rig's reduced mechanism was not forced. A result that appears only after
changing the oracle or budget is exploratory and receives no confirmation
credit. Ordinary target tests outside R1/R2 may inform the dossier but cannot
produce a Reality Bridge terminal result.

Do not commit a foreign repository, dependency tree, generated environment,
or copyrighted source snapshot into `vibe-halt`. Commit only the minimal
provenance, protocol, and bounded evidence permitted by the target license.

### R5 — Integration ratchet and operator handoff

After R1–R3 and any authorized R4:

- add anchored local gates to the single `scripts/gate.sh` writer;
- run targeted Rust and Python tests;
- run the governance and determinism self-tests;
- run the full gate twice from clean state;
- verify frozen Tier-1 identities;
- verify C5/C6 exact behavior and the 29-open-channel ledger;
- inspect `git diff --check`, generated artifacts, and untracked files;
- update current docs only where implementation changed a claim;
- publish bounded draft PRs with exact-head receipts and rollback notes.

Do not combine an external dossier/result with an unrelated refactor. Keep
bridge implementation, corpus/holdout law, and any target result separately
reviewable when ownership or rollback differs.

## 6. Mandatory gate matrix

The final evidence capsule must include exact commands, exits, and commit SHAs
for:

| property | required proof |
|---|---|
| baseline preserved | `make onboard` READY and `make gate` exit 0 before edits |
| Rust truth authority | parser/verification tests reject every R0 malformed or tampered case |
| Python fail-closed | missing engine, timeout, malformed result, unchecked, finding, and mismatch never return clean |
| real positive path | clean/finding/unchecked fixtures are produced by the actual Rust executable |
| cassette honesty | exact replay passes; miss, leftover, malformed, mismatch, timeout, and truncation taint |
| tier honesty | all 29 channels remain open and no public caller can mint D1 |
| holdout honesty | validation proves the candidate-secret freeze commit was human-merged to public `main` before the authenticated reveal and is its Git ancestor; revealed canonical bytes open the curator-authenticated cohort and selection-seed commitments; the fixed sampling frame/cutoff/selection/dedupe/diversity rules reproduce the cohort and screening ledger; independently evidenced curator/evaluator roles differ from the builder; frozen member bytes/IDs/kinds/denominator are immutable; state transitions are append-only; only the first complete pre-frozen run can have `acceptance_credit=true`; changed implementations rerun the revealed cohort only as non-credit calibration; corrections preserve the original; every historical cohort/state/score remains reproducible; misses/nulls/invalids remain visible |
| regression safety | all existing corpus exact counters and controls remain pinned |
| full integration | two consecutive clean full gates on the exact proposed head |

CI on a stale head does not count. A local pass does not count as human
review. A review does not count as merge.

## 7. PR and commit sequence

Prefer this bounded sequence, adapting only for live collisions:

1. **R0/R1 draft PR — strict adapter.** Red-test receipt, Rust machine
   contract, Python client, targeted gates.
2. **R2 draft PR — cooperative transport extraction.** Protocol, fixture,
   leak/taint tests; no grade upgrade.
3. **R3 draft PR — holdout and target admission.** Corpus-owned schema and two
   dossiers; no target execution.
4. **R4 result PR, if authorized.** One target, one fixed protocol, one result.
5. **Integration repair PR only if needed.** No omnibus cleanup.

Each PR begins draft, names its owner and exact base/head SHA, cites the
controller package, lists changed surfaces, includes test receipts, declares
open claims, and states rollback. Marking ready is a human decision. Merging is
human-only.

Do not stack R2 on unmerged R1 if their only dependency is a small interface:
freeze that interface and rebase each bounded PR on current merged main after
the preceding human merge. During R1 review pauses, continue only public,
read-only R3 dossier research; do not write the holdout schema or freeze a
cohort against an unmerged bridge contract.

## 8. Kill and stop rules

Stop the affected lane—without stopping independent safe work—when:

- live main or ownership contradicts this controller;
- baseline `make gate` is red for an unresolved reason;
- the bridge can report clean without a Rust-verified result;
- the design requires parsing human stdout as authority;
- unknown schemas or evidence fields are accepted;
- a D2 path can be labeled D1;
- the cassette extraction weakens C5/C6 or hides an open channel;
- a positive test requires a fabricated success receipt;
- exact target revision, license, oracle, or operator authority is missing;
- foreign execution would require credentials, live services, production
  data, destructive actions, or writes outside a disposable environment;
- frozen Tier-1 identity moves without a predeclared justified schema change;
- a shared-surface or verifier ownership collision lacks an exact interface
  grant;
- two repair loops fail for the same cause without new evidence.

At a stop, emit one packet containing the exact blocker, evidence, smallest
safe options, recommendation, and consequence of waiting. Do not fill time
with speculative features, supervisor work, guided search, dashboards, broad
refactors, or document proliferation.

## 9. Terminal report

The final report must answer, with links and exact SHAs:

1. What was merged on current `origin/main`, what remains draft, and what is
   only local?
2. Can Python return `CLEAN` without Rust-owned verified evidence? Show the
   negative proof.
3. What exact machine protocol and schemas are frozen?
4. Does the reusable cassette path preserve D2 and all 29 open channels?
5. Does the holdout law retain misses and predeclared nulls?
6. Which target dossier is admission-ready?
7. Was foreign execution authorized? If so, what was the predeclared result?
8. How did each of the seven 12-week criteria move?
9. What is the single highest-ROI next build?
10. What, if anything, must the operator merge or authorize next?

The report must use one of these terminal labels:

- `REALITY_BRIDGE_WAVE_A_DRAFT_READY_FOR_HUMAN_REVIEW`
- `REALITY_BRIDGE_WAVE_A_MERGED_READY_FOR_WAVE_B`
- `REALITY_BRIDGE_WAVE_B_DRAFTS_READY_FOR_HUMAN_REVIEW`
- `REALITY_BRIDGE_COMPLETE_FORWARD_CONFIRMED`
- `REALITY_BRIDGE_COMPLETE_FORWARD_NULL`
- `REALITY_BRIDGE_LOCAL_READY_TARGET_AUTHORITY_REQUIRED`
- `REALITY_BRIDGE_BLOCKED_<precise_reason>`

`REALITY_BRIDGE_WAVE_B_DRAFTS_READY_FOR_HUMAN_REVIEW` is valid only when the
R2 and R3 draft PRs are based on their required merged predecessors, their
targeted and full gates are green on the exact heads, their evidence capsules
are attached, no foreign target was executed, and both await human review or
merge. It grants no permission to enter R4.

Anything else is an interim checkpoint, not completion.

---

This controller deliberately makes external contact the next proof while
keeping the engine's epistemic immune system intact. The bridge is successful
when a client can use the rig without becoming a second truth oracle, and when
the first foreign-target result—positive or null—survives the exact protocol
declared before execution.
